name: ci

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    uses: tty-pt/ci/.github/workflows/all-in-one.yml@main
    with:
      name: axil-hyle
      deps: "axil,libqmap,libxylem,libhyle,libhyle-source,axil-auth"
      deps_deb: "axil,libqmap,libxylem,libhyle,libhyle-source,axil-auth,libjson-c-dev"
      deps_apk: "axil,libqmap,libxylem,libhyle,libhyle-source,axil-auth,json-c-dev"
      deps_rpm: "axil,libqmap,libxylem,libhyle,libhyle-source,axil-auth,json-c-devel"
      deps_brew: "axil,libqmap,libxylem,libhyle,libhyle-source,axil-auth,json-c"
      publish_to: "deb,apk,rpm,brew,openbsd"
    secrets:
      HOST: ${{ secrets.OPENBSD_HOST }}
      USER: ${{ secrets.OPENBSD_USER }}
      SSH_KEY: ${{ secrets.OPENBSD_SSH_KEY }}
      GPG_PRIVATE_KEY: ${{ secrets.OPENBSD_GPG_PRIVATE_KEY }}
      GPG_PASSPHRASE: ${{ secrets.OPENBSD_GPG_PASSPHRASE }}
      GPG_KEYID: ${{ secrets.OPENBSD_GPG_KEY_ID }}
      ABUILD_PRIVATE_KEY: ${{ secrets.ABUILD_PRIVATE_KEY }}
      ABUILD_PUBLIC_KEY: ${{ secrets.ABUILD_PUBLIC_KEY }}
      TAP_TOKEN: ${{ secrets.TAP_TOKEN }}
